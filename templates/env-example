# Copy this file to your project's .env and fill in the values.
# The watcher service sources this file before running your deploy script.

# Required by watcher --------------------------------------------------------

# "production" or "development"
# The watcher only runs git fetch+reset when CURRENT_ENV is "production" (or empty).
CURRENT_ENV=production

# Branch the watcher pulls from (default: main)
DEPLOY_BRANCH=main

# Project-specific variables -------------------------------------------------

# TODO: Add your own variables below. Examples:
# DATABASE_URL=postgres://user:pass@db:5432/myapp
# REDIS_URL=redis://redis:6379/0
# SECRET_KEY=change-me
